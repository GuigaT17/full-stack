const SECONDS = 1000
const MINUTES = 600000
const HOURS = MINUTES * 60
const DAYS = HOURS * 24

const REFRESH_RATE = 5 //units of something

const STARTING_BALANCE = 50

module.exports = {
    SECONDS,
    MINUTES,
    HOURS,
    DAYS,
    REFRESH_RATE,
    STARTING_BALANCE
}